greeting:
  message: hello

# 数据库连接配置
quarkus:
  datasource:
    db-kind: mysql
    username: root
    password: 713181
    jdbc:
      driver: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/test
      # 设置数据库连接池大小
      max-size: 20

